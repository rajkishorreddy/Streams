import React from "react";
import Modal from "../Modal";

//modal rendered as a portal at #modal in html
const StreamDelete = () => {
  return (
    <div>
      StreamDelete
      <Modal />
    </div>
  );
};

export default StreamDelete;
